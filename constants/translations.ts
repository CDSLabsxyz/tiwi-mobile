/**
 * Deprecated — kept only as a re-export for any external imports.
 * The canonical translation dictionary lives in `lib/i18n/translations.ts`
 * and is merged from the web app's master dictionary plus RN-specific keys.
 */

export { TRANSLATIONS, type TranslationKey } from '@/lib/i18n/translations';
